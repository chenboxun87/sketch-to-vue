# assets/

可复制到用户项目 `data/` 的 JSON 模板（不含业务数据）。

| 文件 | 用途 |
|------|------|
| `symmetric_module_clones.template.json` | 对称 KPI 模块：`excludeNativeIds` + `specs[]` clone 规格占位 |

配合 `references/symmetric-kpi-override.md` 与 `detect-symmetric-module-gaps.mjs` 使用。
